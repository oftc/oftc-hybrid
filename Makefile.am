AUTOMAKE_OPTIONS = foreign
SUBDIRS = tools libltdl doc etc help lib messages modules servlink src

install-data-local:
	$(INSTALL) -d $(DESTDIR)${localstatedir}/log
	$(INSTALL) -d $(DESTDIR)${localstatedir}/run
